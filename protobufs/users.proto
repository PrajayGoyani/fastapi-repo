syntax = "proto3";

package users;

service UserService {
  rpc GetUserProfile (UserRequest) returns (UserResponse) {}
}

message UserRequest {
  string user_id = 1;
}

message UserResponse {
  string user_id = 1;
  string name = 2;
}